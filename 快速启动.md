# 🚀 快速启动指南

## ✅ 当前状态

- ✅ 代码已迁移到 PostgreSQL + pgvector
- ✅ TypeScript 编译通过
- ✅ 服务器已启动（http://localhost:3000）
- ⚠️ 需要初始化数据库

## 📋 数据库初始化（3种方式任选其一）

### 方式1：使用SQL脚本（推荐）

```bash
# 1. 创建数据库（如果不存在）
sudo -u postgres psql -c "CREATE DATABASE medical_beauty_crm;"

# 2. 执行初始化脚本
sudo -u postgres psql -d medical_beauty_crm -f scripts/init-database.sql
```

### 方式2：使用Drizzle迁移

```bash
# 确保 .env 中的 DATABASE_URL 正确
# 然后运行：
npm run db:push
```

### 方式3：手动连接PostgreSQL

```bash
# 连接到PostgreSQL
sudo -u postgres psql

# 在psql中执行：
CREATE DATABASE medical_beauty_crm;
\c medical_beauty_crm
\i scripts/init-database.sql
\q
```

## 🔧 检查数据库连接

编辑 `.env` 文件，确保 `DATABASE_URL` 正确：

```env
# 如果使用postgres用户（需要密码）
DATABASE_URL=postgresql://postgres:你的密码@localhost:5432/medical_beauty_crm

# 如果使用当前系统用户（peer认证）
DATABASE_URL=postgresql://l@localhost:5432/medical_beauty_crm

# 如果使用socket连接
DATABASE_URL=postgresql:///medical_beauty_crm
```

## 🎯 验证系统

1. **检查数据库表**
   ```bash
   psql $DATABASE_URL -c "\dt"
   ```

2. **检查服务器**
   ```bash
   curl http://localhost:3000
   ```

3. **访问前端**
   打开浏览器访问：http://localhost:3000

## 📝 已配置的功能

- ✅ Airtable API Key: 通过环境变量配置
- ✅ Airtable Base ID: 通过环境变量配置
- ✅ DeepSeek API Key: 通过环境变量配置
- ✅ 企业微信 Webhook: 已实现

## 🐛 常见问题

### 问题1：PostgreSQL连接失败

**解决方案：**
```bash
# 检查PostgreSQL是否运行
sudo systemctl status postgresql

# 如果未运行，启动服务
sudo systemctl start postgresql

# 检查连接
psql -U postgres -d postgres
```

### 问题2：权限不足

**解决方案：**
```bash
# 使用sudo执行
sudo -u postgres psql -d medical_beauty_crm -f scripts/init-database.sql
```

### 问题3：数据库已存在

**解决方案：**
```bash
# 删除旧数据库（谨慎操作）
sudo -u postgres psql -c "DROP DATABASE medical_beauty_crm;"

# 重新创建
sudo -u postgres psql -c "CREATE DATABASE medical_beauty_crm;"
```

## 🎉 完成后的功能

初始化完成后，你可以使用：

1. **AI客服对话** - 前端聊天界面
2. **客户管理** - 线索、画像、分层
3. **知识库** - RAG检索增强生成
4. **企业微信** - Webhook接收、消息发送
5. **Airtable同步** - CRM数据同步
6. **触发器** - 自动化营销

## 📞 需要帮助？

如果遇到问题，检查：
1. PostgreSQL服务是否运行
2. `.env` 文件中的 `DATABASE_URL` 是否正确
3. 数据库用户是否有足够权限
